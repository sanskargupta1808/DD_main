# IndicConformer STT service — Python dependencies
indic-asr-onnx
fastapi==0.115.12
uvicorn[standard]==0.34.2
python-multipart==0.0.12
numpy==1.26.4
scipy==1.14.1
scikit-learn==1.5.2
librosa==0.10.2.post1
resemblyzer==0.1.4
webrtcvad==2.0.10
soundfile==0.12.1
setuptools<81
python-dotenv

# fp32 IndicConformer decoding path (fp32_transcriber.py) — previously
# installed ad hoc and missing from this file; pinning them here so a fresh
# `pip install -r requirements.txt` actually works.
torch
torchaudio
onnxruntime
huggingface_hub

# Acoustic speaker diarization (pyannote engine, alternative to the
# resemblyzer+KMeans one above — see speaker_diarization_pyannote.py).
# Gated on Hugging Face: requires HF_TOKEN and accepting the license for
# pyannote/speaker-diarization-3.1, pyannote/segmentation-3.0, and
# pyannote/speaker-diarization-community-1.
pyannote.audio
