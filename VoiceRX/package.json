{
  "name": "voicerx",
  "version": "0.1.0",
  "private": true,
  "description": "Transcribe doctor-patient conversations and extract structured medical data (symptoms, diagnoses, prescriptions, follow-up).",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "start:all": "./start.sh",
    "build": "npm run build -w server && npm run build -w client",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w client",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18"
  }
}
